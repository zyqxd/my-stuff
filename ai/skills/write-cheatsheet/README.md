# write-cheatsheet (planned)

The skill itself is context-switching **e07 s03**; `SKILL.md` does not exist yet.

`tools/render.mjs` is the s02 spike output — renders an HTML file to a 2× PNG using a
Chromium already cached under `~/Library/Caches/ms-playwright`. It produced the four
diagrams in `~/.brain/memory-bank/personal/knowledge/assets/context-switching/`.
